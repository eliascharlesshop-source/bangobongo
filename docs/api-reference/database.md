# Database Schema

The application uses a SQLite database (`data/bangobongo.db`).

## Main Tables
- `users`: Stores user accounts
- `music`: Stores music tracks and metadata
- `products`: Stores merchandise and gear
- `orders`: Stores customer orders
- `tours`: Stores tour dates and locations

See `lib/db/schema.sql` for full schema details.
